Agent - Manus
확인해보겠습니다.
네, 이게 바로 G마켓이 GitHub Actions 서버 IP를 봇으로 인식해서 차단하는 문제입니다. 상품 목록(li.list-item)이 아예 로딩되지 않고 타임아웃이 발생하고 있어요.
이 문제를 해결하려면 크롤링 방식을 변경해야 합니다. G마켓 베스트 페이지의 데이터를 브라우저 렌더링 없이 API 호출이나 서버사이드 HTML로 가져오는 방식으로 수정하겠습니다. 약 5~10분 소요됩니다.
진행 상황 업데이트입니다. G마켓 베스트 페이지를 분석한 결과:
단순 requests로는 봇 확인 페이지만 반환됨
별도 공개 API는 없고, Next.js 초기 데이터에 상품 목록이 포함되는 구조
현재 requests로 Next.js 초기 데이터를 파싱하는 방식을 우선 시도하고, 안 되면 Playwright stealth 모드를 fallback으로 적용하는 방향으로 수정 중입니다. 조금 더 기다려주세요.
크롤러 수정이 거의 완료된 것 같습니다. requests 기반 Next.js 데이터 파싱 + Playwright stealth fallback 방식으로 코드를 수정하고, README와 requirements.txt도 업데이트 중입니다. 1~2분 내로 저장소에 푸시될 예정입니다.
서브태스크에서 GitHub 인증 문제로 푸시가 막히고 있는 것 같습니다. 제가 직접 이쪽에서 푸시하겠습니다. 잠시만요.
저장소에 수정된 크롤러를 푸시했습니다. 다만 workflow 파일(.github/workflows/gmarket-best-daily.yml)은 권한 제한으로 자동 업데이트가 안 되어서, 이 파일도 수정이 필요합니다.
GitHub에서 직접 .github/workflows/gmarket-best-daily.yml 파일을 열어서 아래 내용으로 교체해주세요:
변경 포인트:
Playwright stealth 의존성 설치 추가
크롤러 실행 명령에서 requests 우선 시도 + stealth fallback 옵션 반영
파일 내용을 보여드릴까요? 아니면 첨부해드릴까요?
첨부해줘
수정된 workflow 파일을 첨부드립니다. GitHub에서 .github/workflows/gmarket-best-daily.yml 파일을 열고 이 내용으로 교체해주세요.
gmarket-best-daily.yml
코드 · 2.33 KB
이 작업의 모든 파일 보기


GitHub


name: Daily Gmarket Best Crawler

on:
  schedule:
    # KST 09:00 = UTC 00:00
    - cron: "0 0 * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  crawl-and-email:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Install virtual display
        run: |
          sudo apt-get update
          sudo apt-get install -y xvfb

      - name: Resolve Chrome or Chromium executable
        run: |
          BROWSER_PATH="$(command -v google-chrome || command -v google-chrome-stable || command -v chromium || command -v chromium-browser || true)"
          if [ -z "$BROWSER_PATH" ]; then
            echo "System Chrome/Chromium was not found. Installing Playwright Chromium as fallback."
            python -m playwright install --with-deps chromium
          else
            echo "BROWSER_EXECUTABLE_PATH=$BROWSER_PATH" >> "$GITHUB_ENV"
            echo "Using browser: $BROWSER_PATH"
          fi

      - name: Crawl Gmarket Best 1-200
        run: |
          if [ -n "${BROWSER_EXECUTABLE_PATH:-}" ]; then
            xvfb-run -a python gmarket_best_crawler.py --max-items 200 --mode auto --headed --browser-executable-path "$BROWSER_EXECUTABLE_PATH"
          else
            python gmarket_best_crawler.py --max-items 200 --mode auto
          fi

      - name: Upload CSV artifact
        uses: actions/upload-artifact@v4
        with:
          name: gmarket-best-csv
          path: output/*.csv
          if-no-files-found: error
          retention-days: 14

      - name: Send CSV by email
        env:
          SMTP_HOST: ${{ secrets.SMTP_HOST }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SMTP_USERNAME: ${{ secrets.SMTP_USERNAME }}
          SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
          SMTP_FROM: ${{ secrets.SMTP_FROM }}
          SMTP_TO: ${{ secrets.SMTP_TO }}
          SMTP_USE_TLS: ${{ secrets.SMTP_USE_TLS }}
          SMTP_USE_SSL: ${{ secrets.SMTP_USE_SSL }}
          EMAIL_SUBJECT: ${{ secrets.EMAIL_SUBJECT }}
        run: python send_email.py
